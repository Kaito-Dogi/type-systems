import {StyleSheet} from 'react-native';

/**
 * @package
 */
export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    justifyContent: 'center',
    width: '100%',
    aspectRatio: 1 / 1,
    backgroundColor: 'red',
  },
  text: {
    fontSize: 16,
    lineHeight: 24,
    color: 'white',
  },
});
